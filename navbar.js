let img = document.querySelector("navbar")
function toggleNav(e) {
  // toggle menubar with the closing icon
  // make visible the navigation
  if (img.src = "assets/menu.png" ) {
    img.src = "assets/closing-icon.svg";
  } 
  console.log();
}